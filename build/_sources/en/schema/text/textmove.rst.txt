.. _en/schema/text/textmove

=========
Move text
=========

For QElectroTech, the text field is considered as an object similar to the rest (elements, line, rectangle, 
ellipse, picture, etc.). The text can be move arround the workspace using the mouse or the keyboard.

Move text field by mouse
~~~~~~~~~~~~~~~~~~~~~~~~

    1. Left clik on the text field which should be moved.
    2. Without releasing, move the mouse to the new desired position.

Move text field by keyboard
~~~~~~~~~~~~~~~~~~~~~~~~~~~

    1. Select the text field which should be moved.
    2. Press the corresponding arrow (Downwards, Rightward, Leftwards or Upwards) to move the text field one grid row or column.
